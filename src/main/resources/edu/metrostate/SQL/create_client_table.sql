CREATE TABLE Client (
    clientID INT AUTO_INCREMENT PRIMARY KEY,
    firstName VARCHAR(50),
    lastName VARCHAR(50),
    phoneNumber VARCHAR(15),
    email VARCHAR(100)
    password VARCHAR(25)
);
